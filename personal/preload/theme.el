;;; theme.el --- Emacs Theme
;;
;;; Commentary:
;; Personal settings for which theme to load

;;; Code:
;;(setq prelude-theme 'dracula)
;;(setq moe-theme-mode-line-color 'blue)
;; (Available colors: blue, orange, green ,magenta, yellow, purple, red, cyan, w/b.)

;; dracula-pro-pro theme
(add-to-list 'custom-theme-load-path "~/.emacs.d/themes")
(setq prelude-theme 'dracula-pro-pro)

;;; theme.el ends here
